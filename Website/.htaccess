RewriteEngine on
RewriteCond %{SERVER_PORT} 80
RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

<Files "offre_data.json">
    Require all denied
</Files>

<Files "config.php">
    Require all denied
</Files>
